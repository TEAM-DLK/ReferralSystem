python-telegram-bot==20.0
pydantic==1.10.13
python-dotenv==0.21.0
sqlite3  # This is typically included with Python, so it might not need to be listed
requests==2.28.1
twine==4.0.1
pytest==7.1.2  # Only if you want to include tests in your repo
